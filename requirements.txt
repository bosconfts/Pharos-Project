# Dependências da API read-only (core/step5_api.py).
#
# Mantido enxuto de propósito: é este arquivo que a Vercel instala, e o limite
# de tamanho de função não comporta torch. A API não roda o pipeline, não gera
# embeddings e não assina transações — o worker é que faz isso, e as
# dependências dele estão em requirements-worker.txt.
fastapi==0.115.0
uvicorn==0.30.6
httpx==0.27.0
python-dotenv==1.0.1
psycopg2-binary
